/*import Foundation
import FirebaseFirestore

let db = Firestore.firestore()
func saveYoutubeVideo(videoURL: String) {
    let db = Firestore.firestore()
    db.collection("categories").addDocument(data: [
        "videoURL": videoURL,
        "createdAt": Timestamp()
    ]) { error in
        if let error = error {
            print("Error adding document: \(error)")
        } else {
            print("Document added successfully!")
        }
    }
}
*/

